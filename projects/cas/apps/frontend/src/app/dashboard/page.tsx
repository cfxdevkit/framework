import { CasConsole } from '../../components/CasConsole';

// biome-ignore lint/style/noDefaultExport: Next.js app router requires a default page export.
export default function DashboardPage() {
  return <CasConsole />;
}
