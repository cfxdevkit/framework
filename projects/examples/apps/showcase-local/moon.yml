$schema: 'https://moonrepo.dev/schemas/project.json'

layer: 'application'
language: 'typescript'

tasks:
  lint:
    command: 'pnpm exec biome check app'
    inputs:
      - 'app/**/*'
      - '/biome.json'
    options:
      mergeArgs: 'replace'
      cache: true
