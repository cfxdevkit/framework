$schema: 'https://moonrepo.dev/schemas/project.json'

layer: 'application'
language: 'typescript'

tasks:
  build:
    command: 'pnpm exec next build --webpack'
    deps:
      - 'showcase-local:build'
    inputs:
      - 'app/**/*'
      - 'package.json'
      - 'next.config.*'
      - 'tsconfig*.json'
    outputs:
      - '.next'
    options:
      runInCI: false
      mergeArgs: 'replace'
      mergeOutputs: 'replace'

  typecheck:
    options:
      runInCI: false

  test:
    options:
      runInCI: false

  check:
    options:
      runInCI: false

  clean:
    options:
      runInCI: false

  lint:
    command: 'pnpm exec biome check app'
    inputs:
      - 'app/**/*'
      - '/biome.json'
    options:
      runInCI: false
      mergeArgs: 'replace'
      cache: true
