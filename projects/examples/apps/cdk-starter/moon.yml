$schema: 'https://moonrepo.dev/schemas/project.json'
layer: 'application'
language: 'typescript'

tasks:
  build:
    options:
      disabled: true
  typecheck:
    options:
      disabled: true
  test:
    options:
      disabled: true
  check:
    options:
      disabled: true
  lint:
    options:
      disabled: true
  clean:
    options:
      disabled: true
