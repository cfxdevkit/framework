/**
 * @cfxdevkit/codegen-contracts — extracts ABI/bytecode from Hardhat artifacts and
 * renders TypeScript modules consumed by `@cfxdevkit/contracts`.
 *
 * Phase A: empty CLI scaffold. Real implementation will live in `extract.ts`
 * and `render.ts` per [tools/STRUCTURE.md](../../STRUCTURE.md).
 */
export const __packageName = '@cfxdevkit/codegen-contracts' as const;
