$schema: 'https://moonrepo.dev/schemas/project.json'

layer: 'tool'
language: 'typescript'

tasks:
  mcp-start:
    script: 'pnpm exec cfxdevkit-mcp'
    options:
      mergeArgs: 'replace'
  mcp-stop:
    script: 'pkill -f cfxdevkit-mcp || true'
    options:
      mergeArgs: 'replace'
