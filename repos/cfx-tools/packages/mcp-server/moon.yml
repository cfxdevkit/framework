$schema: 'https://moonrepo.dev/schemas/project.json'

layer: 'library'
language: 'typescript'

tasks:
  build:
    deps:
      - 'client:build'
      - 'compiler:build'
      - 'cdk:build'
      - 'scaffold-cli:build'
      - 'signer-session:build'

  typecheck:
    deps:
      - 'client:build'
      - 'compiler:build'
      - 'cdk:build'
      - 'scaffold-cli:build'
      - 'signer-session:build'
