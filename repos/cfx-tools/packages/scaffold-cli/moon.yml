$schema: 'https://moonrepo.dev/schemas/project.json'

layer: 'library'
language: 'typescript'

tasks:
  smoketest:
    command: node ../../scripts/smoketest-scaffold.mjs
    deps:
      - ~:build
    options:
      runInCI: true
