---
title: "Documentation & Architecture"
---

import { Mermaid } from '../../components/Mermaid';

# Documentation & Architecture

# Documentation & Architecture Module

## Overview

The Documentation & Architecture module governs the Conflux DevKit monorepo's structural integrity, dependency boundaries, and documentation standards. It is not executable code but a collection of governance artifacts that enforce the 5-tier dependency hierarchy, validate architectural rules at commit time, and maintain the documentation system.

**Key responsibilities:**
- Enforce tier-based dependency rules (no upward imports, no cycles)
- Validate architecture at commit time via `@cfxdevkit/arch-check`
- Maintain GitNexus code intelligence index for impact analysis
- Document architectural decisions via ADRs
- Coordinate LLM automation agents for repository upkeep

## Architecture Model

### 5-Tier Dependency Hierarchy

```
Tier -1  cross-cutting   repos/cfx-meta, repos/cfx-config        (devDep only)
Tier  0  framework       repos/cfx-core, cfx-keys, cfx-ui,       (published npm)
                         cfx-solidity
Tier  1  platform        repos/cfx-tools, repos/cfx-llm          (workspace:*)
Tier  2  domains         repos/cfx-domain                        (workspace:*)
Tier  3  projects        projects/cas, projects/examples, …      (internal)
```

**Dependency direction is strictly downward.** Higher tiers cannot be runtime dependencies of lower tiers. Cross-cutting packages are devDependencies only.

<Mermaid chart={`graph BT
    subgraph "Tier -1: Cross-Cutting"
        A[repos/cfx-meta]
        B[repos/cfx-config]
    end
    
    subgraph "Tier 0: Framework"
        C[repos/cfx-core]
        D[repos/cfx-keys]
        E[repos/cfx-ui]
        F[repos/cfx-solidity]
    end
    
    subgraph "Tier 1: Platform"
        G[repos/cfx-tools]
        H[repos/cfx-llm]
    end
    
    subgraph "Tier 2: Domains"
        I[repos/cfx-domain]
    end
    
    subgraph "Tier 3: Projects"
        J[projects/cas]
        K[projects/examples]
    end
    
    A -->|devDep| C
    A -->|devDep| G
    B -->|devDep| C
    B -->|devDep| G
    
    C -->|npm| G
    D -->|npm| G
    E -->|npm| G
    F -->|npm| G
    
    G -->|workspace| I
    I -->|npm| C
    I -->|npm| D
    I -->|npm| E
    
    J -->|npm| C
    J -->|npm| D
    J -->|npm| E
    J -->|npm| I
    K -->|npm| C
    K -->|npm| D
    K -->|npm| E
`} />

### Boundary Contracts

| Tier | Location | Runtime Deps | Publishes to npm |
|------|----------|--------------|------------------|
| -1 | `repos/cfx-meta`, `repos/cfx-config` | None | No (devDep only) |
| 0 | `repos/cfx-*` | None | Yes (semver) |
| 1 | `repos/cfx-tools`, `repos/cfx-llm` | Tier 0 only | No (workspace:*) |
| 2 | `repos/cfx-domain` | Tier 0 only | Yes (semver) |
| 3 | `projects/*` | Tiers 0-2 | No (internal) |

## Core Components

### `@cfxdevkit/arch-check`

Architecture validation package located at `repos/cfx-tools/packages/arch-check`. Runs via `pnpm arch:check` or as part of `pnpm llm:commit`.

**Checks enforced:**
- `requires-moon-yml` — publishable packages must have `moon.yml`
- `requires-src-index` — publishable packages must have `src/index.ts`
- `no-upward-imports` — no runtime dep on a higher tier
- `no-internal-reach` — no imports into `src/` internals of another package
- `file-size-hard-limit` — source files ≤ 300 lines (excluding `generated/`)
- `no-ts-nocheck` — no `// @ts-nocheck` in source
- `no-js-mjs-source-files` — source must be TypeScript

**Output artifacts:**
- `artifacts/llm/reports/arch-check-report.{json,md}`
- `artifacts/llm/reports/code-hotspots.{json,md}`
- `artifacts/llm/reports/docs-alignment.{json,md}`

### GitNexus Code Intelligence

GitNexus indexes the full call graph and exposes it via MCP tools. Index stats: 14,697 symbols · 22,936 relationships · 300 execution flows.

**MCP Resources:**
| Resource URI | Purpose |
|---|---|
| `gitnexus://repo/framework/context` | Codebase overview, index freshness |
| `gitNexus://repo/framework/clusters` | All functional areas / clusters |
| `gitnexus://repo/framework/processes` | All named execution flows |
| `gitnexus://repo/framework/process/{name}` | Step-by-step trace of one flow |

**MCP Tools:**
| Tool | When to use |
|------|-------------|
| `mcp_gitnexus_query` | Explore a concept / feature area |
| `mcp_gitnexus_context` | Full callers + callees + execution flows for one symbol |
| `mcp_gitnexus_impact` | Blast radius before editing — direction: `upstream` |
| `mcp_gitnexus_detect_changes` | Pre-commit: verify only expected symbols changed |
| `mcp_gitnexus_rename` | Rename a symbol safely across the call graph |

**CLI commands:**
```bash
npx gitnexus analyze      # re-index (must run first if index is stale)
npx gitnexus status       # freshness info
npx gitnexus list         # list all tracked repos
```

**Workflow rule:** ALWAYS run `mcp_gitnexus_impact` before editing any function/class/method. If risk is HIGH or CRITICAL, warn and confirm before proceeding. ALWAYS run `mcp_gitnexus_detect_changes` before committing.

### OpenSpec Change Management

OpenSpec structures work into artifacts before code is written. All changes live under `openspec/changes/<name>/`.

**Artifact flow:**
```
proposal.md  →  design.md  →  specs/  →  tasks.md
 (why/what)      (how)      (per-cap)   (checklist)
```

**CLI commands:**
```bash
openspec new change "<kebab-name>"   # scaffold a new change
openspec list --json                  # list active changes
openspec status --change "<name>" --json
openspec instructions <artifact> --change "<name>" --json
openspec instructions apply --change "<name>" --json
```

**Agent slash commands:**
| Command | Action |
|---------|--------|
| `/opsx:explore` | Thinking-partner mode |
| `/opsx:propose <name>` | Generate all artifacts in one step |
| `/opsx:apply [name]` | Implement pending tasks |
| `/opsx:archive [name]` | Archive a completed change |

### Architectural Decision Records (ADRs)

Located in `docs/adr/`. ADRs are append-only; superseded ADRs link forward to their replacement.

**Current ADRs:**
| ADR | Status | Purpose |
|-----|--------|---------|
| 0001-build-stack.md | Accepted | Build & workspace stack (pnpm, moon, Vite, Biome) |
| 0002-keystore.md | Accepted | Keystore strategy (multi-backend, session keys) |
| 0003-multi-repo-split.md | Proposed | Multi-repo split by technical surface |

## Documentation Structure

### Current Layout

```
docs/
├── README.md
├── STRUCTURE.md
├── architecture/
│   ├── framework-design-principles.md
│   ├── framework-error-types.md
│   ├── keystore-session-provider.md
│   └── package-layout.md
├── adr/
│   ├── 0001-build-stack.md
│   ├── 0002-keystore.md
│   └── 0003-multi-repo-split.md
├── keystore-docker.md
├── llm-automation-agents.md
└── llm-fine-tuning-plan.md
```

### Target Layout

```
docs/
├── architecture/           Cross-cutting design docs
├── adr/                    Architectural Decision Records
├── guides/                 How-to guides
├── api/                    Generated API reference (TypeDoc)
├── projects/               Per-project documentation
├── security/               Security material
└── reference/              Misc reference material
```

## Build & Validation Stack

| Tool | Role | Config Location |
|------|------|-----------------|
| **pnpm** | Package manager, workspace protocol | `pnpm-workspace.yaml` |
| **moonrepo** | Task runner, build cache | `.moon/workspace.yml`, per-package `moon.yml` |
| **Vite 7** | Library + app builds | per-package `vite.config.ts` |
| **Biome** | Lint + format | `biome.json` |
| **arch-check** | Architecture validation | `repos/cfx-tools/packages/arch-check` |
| **GitNexus** | Code intelligence | `gitnexus` CLI / MCP tools |

**Key root scripts:**
```bash
pnpm build            # moon run :build
pnpm test             # moon run :test --concurrency 1
pnpm lint             # moon run :lint
pnpm typecheck        # moon run :typecheck
pnpm arch:check       # arch-rules + import-boundary checks
pnpm check:hotspots   # file size + git churn analysis
pnpm check:secrets    # secret pattern scanning
pnpm check:docs       # markdown path + moon.yml alignment
pnpm check:corpus     # rebuild LLM corpus for AI agents
```

## Package Conventions

Every package follows this standard layout:

```
<package>/
├── README.md            Scope, public API, deps, examples
├── CHANGELOG.md         Changesets-managed (Tier 0 only)
├── package.json         Name, exports map, peer deps, scripts
├── tsconfig.json        Extends @cfxdevkit/tsconfig/<flavor>.json
├── vite.config.ts       Library build (Tier 0/2) or app build
├── moon.yml             Task definitions (build, test, typecheck, lint)
├── src/
│   ├── index.ts         Public entrypoint — re-exports only, no logic
│   └── <feature>/       One folder per concern
│       ├── index.ts
│       ├── <feature>.ts
│       └── <feature>.test.ts
└── dist/                Build output (gitignored)
```

**Rules:**
- `src/index.ts` is re-exports only
- Named exports only (no default exports in published packages)
- `exports` map in `package.json` is the sole public API surface
- No imports into another package's `src/` internals

## LLM Automation Agents

Located in `repos/cfx-llm/packages/llm-tools`. Implements deterministic repository upkeep agents.

**Agent responsibilities:**
| Agent | Purpose | Output |
|-------|---------|--------|
| `check:corpus` | Build safe repository metadata | `artifacts/llm/corpus/*.jsonl` |
| `check:ci` | Check CI/CD workflow, docs image, release | `artifacts/llm/reports/ci-cd.*` |
| `check:docs` | Detect broken documentation references | `artifacts/llm/reports/docs-alignment.*` |
| `llm:review` | Inspect current git changes | `artifacts/llm/reports/review.*` |
| `check:hotspots` | Scan source file size and churn | `artifacts/llm/reports/code-hotspots.*` |
| `check:eval` | Summarize deterministic gates | `artifacts/llm/reports/eval.*` |

**Commands:**
```bash
pnpm run llm:all
pnpm run llm:models
pnpm run llm:actions
pnpm run llm:review
pnpm run check:corpus
pnpm run check:docs
pnpm run check:hotspots
```

## Security Boundaries

| Concern | Located in | Notes |
|--------|-----------|-------|
| Private keys / keystores | never committed | env + `infrastructure/secrets/` references only |
| Smart-contract source | `projects/<p>/contracts/` | each project owns its deployments |
| Generated ABIs | `framework/contracts/` (shared) or per-project | published, versioned |
| Network endpoints | `framework/core` config or per-project env | no hard-coded mainnet keys |
| MCP / AI tool surface | `platform/mcp-server/` | explicit tool allowlist |

## Multi-Repo Split (Planned)

ADR-0003 proposes splitting the monorepo into 6 first-class repositories:

| # | Repo | Owns | Why standalone |
|---|------|------|----------------|
| 1 | `cfx-meta` | Architecture docs, ADRs, release orchestration | Single source of truth |
| 2 | `cfx-core` | Chain primitives, protocol, contracts | Large surface, rarely changes |
| 3 | `cfx-keys` | Keystore, wallet, session keys | Audit-grade trust boundary |
| 4 | `cfx-ui` | React, theme, wallet-connect | UI moves fastest |
| 5 | `cfx-domain` | game-engine, automation | Vertical concerns |
| 6 | `cfx-tools` | scaffold-cli, mcp-server, vscode-extension | Developer experience |

**Migration plan:**
1. Carve `cfx-keys` first (smallest, highest-value)
2. Carve `cfx-tools` (lowest risk)
3. Carve `cfx-domain`
4. Carve `cfx-ui`
5. Rename root → `cfx-core`
6. Bootstrap `cfx-meta`

## Key Files

| File | Purpose |
|------|---------|
| `ARCHITECTURE.md` | Human-readable architecture overview |
| `repos/cfx-meta/arch-rules.yaml` | Machine-readable tier rules — source of truth |
| `pnpm-workspace.yaml` | Workspace package globs |
| `.moon/workspace.yml` | Moon project registry |
| `biome.json` | Root Biome config |
| `tsconfig.base.json` | Root TypeScript base config |
| `openspec/config.yaml` | OpenSpec schema + change root config |
| `CLAUDE.md` / `AGENTS.md` | AI agent instructions |
| `artifacts/llm/reports/` | Latest arch-check, hotspots, docs-alignment reports |
| `artifacts/llm/corpus/` | LLM corpus bundles for AI agents |

## Navigation Quickstart

### "How does X work?"
1. `mcp_gitnexus_query({ query: "concept" })` — find relevant execution flows
2. `mcp_gitnexus_context({ name: "FunctionName" })` — full callers + callees

### "What breaks if I change X?"
1. `mcp_gitnexus_impact({ target: "symbolName", direction: "upstream" })` — blast radius
2. Review risk level; if HIGH/CRITICAL, confirm before editing

### "Is my change safe to commit?"
1. `pnpm arch:check` — tier rules + import boundaries
2. `mcp_gitnexus_detect_changes()` — verify only expected symbols changed
3. `pnpm llm:commit` — full quality gate (arch + review + changeset suggestions)

### "Find where a type is used"
- `vscode_listCodeUsages({ symbol: "TypeName", ... })` — IDE-aware reference search
- or `mcp_gitnexus_context({ name: "TypeName" })` — call-graph-aware

### "What openspec changes are in flight?"
```bash
openspec list --json
```
