import { describe, it } from 'vitest';

describe('@cfxdevkit/client', () => {
  it('runs suite partitions via sibling *.test.ts files', () => {
    // Intentionally empty: Vitest discovers the real suites from the split test modules.
  });
});
