import { describe, it } from 'vitest';

describe('@cfxdevkit/devnode-server', () => {
  it('runs suite partitions via sibling *.test.ts files', () => {
    // Intentionally empty: the real suites live in the split test modules.
  });
});
