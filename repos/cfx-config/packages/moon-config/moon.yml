$schema: 'https://moonrepo.dev/schemas/project.json'

layer: 'configuration'
language: 'typescript'

tasks:
  build:
    command: 'noop'
    options:
      cache: false
  test:
    command: 'noop'
    options:
      cache: false
  typecheck:
    command: 'noop'
    options:
      cache: false
  lint:
    command: 'noop'
    options:
      cache: false
  clean:
    command: 'noop'
    options:
      cache: false
