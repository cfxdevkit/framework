# cfx-meta

Architecture & release orchestration. Holds no shippable code.

After the carve-out this repo will own:

- `ARCHITECTURE.md`, `SECURITY.md`, `CONTRIBUTING.md`
- `docs/adr/` — accepted architecture decisions
- `docs/architecture/` — design notes


- Monthly **integration tags** (`integration-YYYY-MM`) recording which
  versions of each repo were tested together

While in `root/`, those documents live at the repo root and at `docs/`.
This folder exists to mark the carve-out target.
