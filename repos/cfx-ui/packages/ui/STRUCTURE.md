```text
.gitignore — Git ignore rules
API.md — API documentation
README.md — Package overview and usage
STRUCTURE.md — This file: directory layout documentation
moon.yml — Moon repo configuration
package.json — Package metadata and dependencies
src
  AppShell.tsx — Main application shell layout component
  AssetConversionPanel.tsx — UI for asset conversion workflow
  Field.tsx — Generic form field wrapper
  IconButton.tsx — Reusable icon-only button
  Metric.tsx — Display metric value with label
  NetworkSwitchNotice.tsx — Notification for network switching
  Notice.tsx — Generic notice/alert component
  Panel.tsx — Generic panel container
  SegmentedControl.tsx — Tab-like segmented control
  StatusGrid.tsx — Grid layout for status indicators
  TokenAmountField.tsx — Token amount input with formatting
  TokenPairSelector.tsx — UI for selecting token pairs
  TokenSelect.tsx — Dropdown for selecting tokens
  WalletButton.tsx — Wallet connection button
  WalletPickerModal.tsx — Modal for wallet selection
  WalletProviderCard.tsx — Card UI for wallet provider options
  WalletStatusChip.tsx — Status indicator for wallet connection
  components.test.tsx — Component tests entry point
  data-display.ts — Re-exports for data-display components
  feedback.ts — Re-exports for feedback components (notices, etc.)
  form.ts — Re-exports for form components (fields, etc.)
  index.ts — Public API entry point
  panel.ts — Re-exports for panel components
  shell.ts — Re-exports for shell/layout components
  wallet.ts — Re-exports for wallet-related components
dist — Compiled output (generated at build time)
tsconfig.json — TypeScript configuration
vite.config.ts — Vite build configuration
```

<!-- structure-status: enriched -->
<!-- structure-hash: aa86281141091c35c60e3a1bf53dc31acffa4673fd04552aa5c66f3bcc677ff4 -->
